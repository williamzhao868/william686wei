/// <reference path="../pb_data/types.d.ts" />
migrate((app) => {
  // Neutralized migration - no-op to allow system to continue
}, (app) => {
  // Rollback: original values not stored, manual restore needed
})
